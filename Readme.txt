1. C Program to count the number of words, characters and lines in a file
2. C Program to count the number of words, characters and lines in a file using multiple child processes:
The single process approach cannot scale to handle large files. This program receives the number of child processes and an input file name
through the command-line argument.
3. In real word, forks or process crash but that doesnt mean, the program should also crash. Introduced crash handling to the 
multiprocessor word counter.  
